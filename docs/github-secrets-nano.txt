# Copy these names into GitHub → Settings → Secrets and variables → Actions → New repository secret
# Fill values here for your reference; GitHub UI is used to actually add them.

# REQUIRED (CI/CD + Backend)
DATABASE_URL=
REDIS_URL=
CORS_ORIGIN=
METRICS_TOKEN=
ADMIN_SESSION_SECRET=
FAN_SESSION_SECRET=

# OPTIONAL (Prisma)
DATABASE_SHADOW_URL=

# OPTIONAL (post-deploy health probe)
HEALTH_URL=

# OPTIONAL (Kubernetes apply via CI)
KUBE_CONFIG_B64=
GHCR_TOKEN=
INSTALL_CERT_MANAGER=1
CERT_MANAGER_EMAIL=
INGRESS_HOST=
TLS_SECRET=

# Hints
# - Generate strong tokens (macOS/Linux): openssl rand -hex 32
# - KUBE_CONFIG_B64: base64 encode your kubeconfig
# - CORS_ORIGIN: comma-separated origins, no wildcard in prod
