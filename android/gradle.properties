org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# Signing + API credentials are sourced from Gradle properties or CI secrets.
# Example keys (populate via ~/.gradle/gradle.properties, android/local.properties, or CI env):
# android.keystore.path=/path/to/release.keystore
# android.keystore.password=changeit
# android.keystore.alias=gikundiro
# android.keystore.key_password=changeit
# supabase.url=https://paysnhuxngsvzdpwlosv.supabase.co
# supabase.service_key=service-role-key
# api.base_url=https://api.gikundiro.com
