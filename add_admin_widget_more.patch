*** Begin Patch
*** Update File: app/more/page.tsx
@@
+// This page is a client component because it uses useSession
+'use client';
@@
-import MoreDashboard from '@/app/_components/more/MoreDashboard';
+import MoreDashboard from '@/app/_components/more/MoreDashboard';
+import Link from 'next/link';
+import { useSession } from 'next-auth/react';
@@
 export default function MorePage() {
+  // Access the current session to determine the user role
+  const { data: session } = useSession();
@@
   return (
     <>
       {/* Render your existing More page content */}
       <MoreDashboard />
+
+      {/* Render an Admin Panel widget only for system administrators */}
+      {session?.user?.role === 'SYSTEM_ADMIN' && (
+        <section className="mt-6 rounded-lg border border-primary-200 bg-primary-50 p-4">
+          <h2 className="mb-2 text-lg font-bold">Admin Panel</h2>
+          <p className="mb-4 text-sm text-primary-700">
+            Access administrative tools and settings.
+          </p>
+          <Link
+            href="/admin"
+            className="inline-flex items-center rounded-md bg-primary-600 px-4 py-2 font-semibold text-white hover:bg-primary-700"
+          >
+            Open Admin Panel
+          </Link>
+        </section>
+      )}
     </>
   );
 }
*** End Patch
