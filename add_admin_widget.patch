*** Begin Patch
*** Update File: app/(routes)/more/page.tsx
@@
+import Link from 'next/link';
+import { useSession } from 'next-auth/react';
@@
 export default function Page() {
+  // Access current session to determine if user is a system administrator
+  const { data: session } = useSession();
@@
   return (
     <>
       {/* existing More page content */}
+      {/* Show an admin widget if the current user has a SYSTEM_ADMIN role */}
+      {session?.user?.role === 'SYSTEM_ADMIN' && (
+        <section className="mt-6 p-4 rounded-lg border border-primary-200 bg-primary-50">
+          <h2 className="mb-2 text-lg font-bold">Admin Panel</h2>
+          <p className="mb-4 text-sm text-primary-700">
+            Access administrative tools and settings.
+          </p>
+          <Link href="/admin" className="btn">
+            Open Admin Panel
+          </Link>
+        </section>
+      )}
     </>
   );
 }
*** End Patch
