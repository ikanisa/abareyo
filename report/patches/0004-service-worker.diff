diff --git a/public/offline.html b/public/offline.html
new file mode 100644
index 0000000..b0f8d0d
--- /dev/null
+++ b/public/offline.html
@@
+<!doctype html>
+<html lang="en">
+  <head>
+    <meta charset="utf-8" />
+    <meta name="viewport" content="width=device-width,initial-scale=1" />
+    <title>GIKUNDIRO – Offline</title>
+    <style>
+      body {
+        font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
+        margin: 0;
+        min-height: 100vh;
+        display: flex;
+        align-items: center;
+        justify-content: center;
+        background: #040f2a;
+        color: #f8fafc;
+        text-align: center;
+        padding: 2rem;
+      }
+      h1 {
+        font-size: 1.75rem;
+        margin-bottom: 0.5rem;
+      }
+      p {
+        max-width: 28rem;
+        line-height: 1.5;
+        color: rgba(248, 250, 252, 0.78);
+      }
+    </style>
+  </head>
+  <body>
+    <main>
+      <h1>You are offline</h1>
+      <p>
+        We could not reach the Rayon Sports servers. Your recent content is still saved locally. Please
+        reconnect to refresh scores, tickets, and rewards.
+      </p>
+    </main>
+  </body>
+</html>
diff --git a/public/service-worker.js b/public/service-worker.js
index 79eb5aa..a7f5518 100644
--- a/public/service-worker.js
+++ b/public/service-worker.js
@@
-const WEB_PUSH_SUBSCRIPTION_ENDPOINT = '/api/notifications/subscription';
-let webPushPublicKey = null;
+const WEB_PUSH_SUBSCRIPTION_ENDPOINT = '/api/notifications/subscription';
+let webPushPublicKey = null;
+const OFFLINE_FALLBACK_URL = '/offline.html';
+const SHELL_CACHE = 'rayon-shell';
+
+self.addEventListener('install', (event) => {
+  event.waitUntil(
+    caches.open(SHELL_CACHE).then((cache) => cache.add(OFFLINE_FALLBACK_URL)).catch(() => undefined),
+  );
+  self.skipWaiting();
+});
+
+self.addEventListener('activate', (event) => {
+  event.waitUntil(self.clients.claim());
+});
@@
-self.addEventListener('message', (event) => {
-  if (event.data?.type === 'web-push:set-public-key') {
-    webPushPublicKey = event.data.key;
-  }
-});
+self.addEventListener('message', (event) => {
+  if (event.data?.type === 'web-push:set-public-key') {
+    webPushPublicKey = event.data.key;
+  }
+  if (event.data?.type === 'service-worker:skip-waiting') {
+    self.skipWaiting();
+  }
+});
@@
   event.waitUntil(
     self.clients
       .matchAll({ type: 'window', includeUncontrolled: true })
@@
   );
 });
+
+if (self.workbox) {
+  workbox.routing.setCatchHandler(async ({ event }) => {
+    if (event.request.destination === 'document') {
+      const cache = await caches.open(SHELL_CACHE);
+      const offlineResponse = await cache.match(OFFLINE_FALLBACK_URL);
+      if (offlineResponse) {
+        return offlineResponse;
+      }
+      return Response.redirect('/');
+    }
+    return Response.error();
+  });
+}
