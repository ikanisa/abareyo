*** Begin Patch
*** Update File: .env
@@
 OPENAI_API_KEY=<insert-openai-api-key>
 SMS_WEBHOOK_TOKEN=<insert-sms-webhook-token>
 APP_BASE_URL=https://rayon.app
@@
 REALTIME_SIGNING_SECRET=<insert-realtime-signing-secret>
 ANALYTICS_WRITE_KEY=
+
+# ----------------------------------------------------------------------------
+# Default system administrator credentials for the admin console
+#
+# When these variables are defined, the backend will automatically
+# bootstrap a system administrator account if one does not already exist.  The
+# first time you sign in on the admin portal using the email and password
+# specified here, the user will be created with the SYSTEM_ADMIN role and
+# granted full permissions.
+#
+# IMPORTANT: these values should never be committed to a public repository.
+# Set them in your local `.env` for development and in the production secret
+# manager for your hosting platform.
+ADMIN_DEFAULT_EMAIL="info@ikanisa.com"
+ADMIN_DEFAULT_PASSWORD="MoMo!!0099"
+ADMIN_DEFAULT_NAME="System Admin"
*** End Patch
