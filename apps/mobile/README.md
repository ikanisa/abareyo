# Mobile App Placeholder

This workspace will house the Expo/React Native application once the mobile widgets have been realigned.

The shipping mobile codebase remains in `packages/mobile` and `ios`/`android`. Hold migrations here until the shared widget library reaches parity.
