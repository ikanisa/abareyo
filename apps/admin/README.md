# Admin App Placeholder

This directory is reserved for the standalone admin dashboard that will be split out of the monolithic Next.js surface.

Legacy admin routes continue to live under `app/admin` and `app/(routes)/admin`. Defer migrations until the feature set matches the production flows.
