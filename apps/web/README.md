# Web App Placeholder

This directory will host the Next.js web application once the transitional refactor is complete.

For now, the production implementation continues to live under `app/` at the repository root. Move files here only after the parity review concludes.
